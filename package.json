{
  "name": "lifesaver-ai-root",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && node index.js",
    "backend": "cd backend && node index.js",
    "frontend": "cd frontend && npm run dev",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
